## What to build

当已选 Thread 处于 idle 状态时，允许用户把当前 draft 作为普通输入发送。输入被接受后只清空该 Thread 的 draft；输入被拒绝时保留 draft，并在 timeline 外报告失败。

## Blocked by

- 003-restore-message-history.md
- 009-per-thread-draft-lifecycle.md
