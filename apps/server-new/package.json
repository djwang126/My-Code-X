{
  "name": "@my-code-x/server-new",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.build.json",
    "test": "tsx --test --test-reporter=dot src/**/*.test.ts",
    "lint": "eslint \"src/**/*.ts\""
  },
  "license": "AGPL-3.0-only"
}
